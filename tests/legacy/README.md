# Legacy test archive

Status: archived.

`tests/legacy/v2` contains historical tests for the retired API v2. They are
not part of the current test suite and are not guaranteed to run against the
current implementation.

Current acceptance tests belong in `tests/v3`.
